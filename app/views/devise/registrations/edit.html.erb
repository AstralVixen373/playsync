<h2><%= t("devise.registrations.edit.title") %></h2>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <div id="error-notification">
    <%= f.error_notification %>
  </div>

  <div class="form-inputs">
    <% if resource.avatar.attachment&.persisted? %>
      <%= image_tag resource.avatar, class: "rounded-circle mb-2", style: "width: 120px; height: 120px;", alt: "Current avatar" %>
    <% end %>
    <%= f.input :avatar, as: :file, label: "Avatar", input_html: { accept: "image/*" } %>

    <%= f.input :username, required: false, autofocus: true %>

    <fieldset data-controller="preferences" style="border: 1px solid #e0e0e0; border-radius: 6px; padding: 1rem; margin: 1rem 0;">
      <legend style="font-weight: bold; padding: 0 0.5rem;">🎮 <%= t("devise.registrations.edit.filter_preferences") %></legend>
      <div style="display: flex; justify-content: space-between; align-items: baseline; gap: 1rem;">
        <p style="color: #888; font-size: 0.85rem; margin-top: 0;">
          <%= t("devise.registrations.edit.filter_hint") %>
        </p>
        <%= button_tag t("devise.registrations.edit.reset_filters"), type: "button",
              data: { action: "preferences#reset" },
              style: "border: none; background: none; color: #007bff; cursor: pointer; padding: 0; white-space: nowrap;" %>
      </div>

      <div style="margin-bottom: 1rem;">
        <label style="display: block; margin-bottom: 0.5rem; font-weight: bold;"><%= t("devise.registrations.edit.preferred_games") %></label>
        <input type="text" id="profile-game-search" placeholder="<%= t("devise.registrations.edit.search_game") %>"
               autocomplete="off"
               style="width:100%; padding:0.5rem; border: 1px solid #e0e0e0; border-radius: 4px;">
        <div id="profile-game-results"></div>
        <div id="profile-game-chips" data-field="user[preferred_game_ids][]"
             style="display: flex; flex-wrap: wrap; gap: 0.4rem; margin-top: 0.5rem;">
          <%# Always submit at least an empty value so games can be cleared. %>
          <input type="hidden" name="user[preferred_game_ids][]" value="">
          <% resource.preferred_games.each do |game| %>
            <span class="game-chip" style="display: inline-flex; align-items: center; gap: 0.3rem; background: #eef; border-radius: 12px; padding: 0.15rem 0.6rem; font-size: 0.85rem;">
              <input type="hidden" name="user[preferred_game_ids][]" value="<%= game.id %>">
              <%= game.name %>
              <button type="button" class="game-chip-remove" style="border: none; background: none; cursor: pointer; font-weight: bold;">×</button>
            </span>
          <% end %>
        </div>
      </div>

      <div style="margin-bottom: 1rem;">
        <label style="display: block; margin-bottom: 0.5rem; font-weight: bold;"><%= t("devise.registrations.edit.preferred_platforms") %></label>
        <%= render "shared/multiselect", name: "user[preferred_platforms][]", options: Post::PLATFORMS, selected: resource.preferred_platforms, placeholder: t("devise.registrations.edit.no_preference"), include_hidden: true %>
      </div>

      <div style="margin-bottom: 1rem;">
        <label style="display: block; margin-bottom: 0.5rem; font-weight: bold;"><%= t("devise.registrations.edit.preferred_types") %></label>
        <%= render "shared/multiselect", name: "user[preferred_post_types][]", options: Post::TYPES, selected: resource.preferred_post_types, placeholder: t("devise.registrations.edit.no_preference"), include_hidden: true %>
      </div>

      <%= f.input :preferred_language,
                  collection: Post::LANGUAGES.map { |l| [t("post_languages.#{l}"), l] },
                  label_method: :first,
                  value_method: :last,
                  include_blank: t("devise.registrations.edit.no_preference"),
                  label: t("devise.registrations.edit.preferred_language") %>
    </fieldset>
  </div>

  <div class="form-actions">
    <%= f.button :submit, t("devise.registrations.edit.submit") %>
  </div>
<% end %>

<div style="margin-top: 2rem;">
  <%= link_to t("devise.registrations.edit.sign_out"), destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-secondary" %>
</div>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const errorFlash = document.getElementById("error-notification");

    if (errorFlash && errorFlash.innerText.trim() !== "") {
      setTimeout(() => {
        errorFlash.style.transition = "opacity 0.5s ease";
        errorFlash.style.opacity = "0";
        setTimeout(() => { errorFlash.remove(); }, 500);
      }, 5000);
    }
  });
</script>
